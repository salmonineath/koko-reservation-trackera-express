
DATABASE_URL=

JWT_SECRET=
ACCESS_TOKEN_EXPIRES_IN=
REFRESH_TOKEN_TTL_DAYS=

# Single browser origin allowed to call the API with credentials (cookies).
# Local example: http://localhost:5173
# Prod example:  https://app.koko.example.com
FRONTEND_ORIGIN=

# Only used by `npm run db:seed` / `npx prisma db seed` - not read by the app itself.
ADMIN_EMAIL=
ADMIN_PASSWORD=
